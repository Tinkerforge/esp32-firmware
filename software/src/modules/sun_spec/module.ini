[Dependencies]
Requires = Task Scheduler
           Event Log
           API
           Modbus TCP Client

Optional = WS

After    = Modbus TCP Client
           ; Uses esp_random(). Entropy is created by the wifi modem.
           Wifi

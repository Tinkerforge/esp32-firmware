[Dependencies]
Requires = Event Log
           Rtc
           Task Scheduler

Optional = Debug

; The task scheduler has no dependencies before the loop
; and starts the handling of the pre_reboot phase
Before   = *

[Options]
task_scheduler_cpu_idle = 0

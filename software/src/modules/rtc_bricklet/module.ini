[Dependencies]
Requires = Event Log
           API
           NTP
           Rtc

Optional = Io Scheduler

After = Rtc
        NTP # NTP sets the timezone in ::setup(). RTC backends log a timestamp in ::setup().

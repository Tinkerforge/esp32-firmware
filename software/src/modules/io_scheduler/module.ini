[Dependencies]
Requires = Task Scheduler
           Event Log
Optional = Watchdog
           Debug
; The IO task takes over HAL ownership from the main task after the
; board module's setup() has run.
After    = ESP32 Brick
           ESP32 Ethernet Brick

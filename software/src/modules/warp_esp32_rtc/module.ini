[Dependencies]
Requires = Task Scheduler
           Event Log
           API
           NTP
           Rtc
           Warp Esp32 I2c

After    = Warp Esp32 I2c
           NTP # NTP sets the timezone in ::setup(). RTC backends log a timestamp in ::setup().

/** @jsxImportSource preact */
import { h } from "preact";
let x = {
    "require_meter": {
        "cron": {
            "cron_trigger_text": "Wenn die Zählerüberwachung auslöst, ",
            "require_meter": "Zählerüberwachung"
        }
    }
}

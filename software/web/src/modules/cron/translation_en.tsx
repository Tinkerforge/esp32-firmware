/** @jsxImportSource preact */
import { h } from "preact";
let x = {
    "cron": {
        "navbar": {
            "cron": "Events"
        },
        "content": {
            "cron": "Events",
            "task": "Task",
            "condition": "Condition",
            "action": "Action",
            "add_rule": "Regel hinzufügen",
            "edit_rule": "Edit Rule",
            "select": "Select..."
        }
    }
}

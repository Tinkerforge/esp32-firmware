/** @jsxImportSource preact */
import { h } from "preact";
let x = {
    "cron": {
        "navbar": {
            "cron": "Automation"
        },
        "content": {
            "cron": "Automation",
            "rule": "Rule",
            "condition": "Condition",
            "action": "Action",
            "add_rule": "Add rule",
            "edit_rule": "Edit rule",
            "select": "Select..."
        }
    }
}

export interface time {
    year: number
    month: number
    day: number
    hour: number
    minute: number
    second: number
    weekday: number
}

export interface config {
    auto_sync: boolean
}

export interface config {
    sku_override: string,
}

export interface state {
    sku: string,
    sku_product: number,
    sku_model: number,
    sku_material: number,
    sku_type2: number,
    sku_engraving: number,
}

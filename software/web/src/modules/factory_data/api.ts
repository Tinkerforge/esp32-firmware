export interface config {
    sku_override: string,
}

export interface state {
    sku: string,
    sku_product: number,
    sku_generation: number,
    sku_model: number,
    sku_material: number,
    sku_type2_power: number,
    sku_type2_length: number,
    sku_engraving: number,
}
